apiVersion: apps/v1
kind: Deployment
metadata:
  name: lpg-api-deployment
  namespace: test
spec:
  replicas: 3
  selector:
    matchLabels:
      app: lpg-api
  template:
    metadata:
      labels:
        app: lpg-api
    spec:
      containers:
      - name: lpg-api
        image: lpg_api:${{inputs.params.imageTag}}
        ports:
        - containerPort: 80
        volumeMounts:
        - name: data
          mountPath: /data  # Mount path in the container
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: lpg-api-pvc
